{
	"name": "vaults-sdk",
	"targets": {
		"build": {
			"executor": "nx:run-commands",
			"options": {
				"command": "bun i && bun run build",
				"cwd": "drift-vaults/ts/sdk"
			},
			"outputs": [
				"{workspaceRoot}/dist"
			]
		},
		"link": {
			"executor": "nx:run-commands",
			"options": {
				"command": "bun link",
				"cwd": "drift-vaults/ts/sdk"
			},
			"dependsOn": [
				"build"
			]
		}
	}
}