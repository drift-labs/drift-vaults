{
  "name": "vaults-sdk",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun install && bun run build && bun link",
        "cwd": "drift-vaults/ts/sdk"
      },
      
      "outputs": ["dist"]
    }
  }
}
